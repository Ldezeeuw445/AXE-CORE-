# AXE Surface — design pack

See uploaded reference: panels float on `#000`, hairline borders, one cyan accent.

| File | Purpose |
|---|---|
| `axe-surface.css` | Tokens + plain CSS primitives |
| `../presentation/components/surface/Surface.tsx` | React `Panel`, `Ground`, `Chip`, etc. |

Browser uses this via `@import` in `src/app/index.css` and Tailwind extensions in `tailwind.config.js`.
