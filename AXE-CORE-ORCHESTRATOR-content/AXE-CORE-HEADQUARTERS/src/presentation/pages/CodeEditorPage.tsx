/**
 * CodeEditorPage — temporary restore shim.
 * Full IDE lives in CodeEditorPageFull.tsx; this re-exports it.
 * (Recovery from accidental overwrite of the main page file.)
 */
export { default } from './CodeEditorPageFull';
