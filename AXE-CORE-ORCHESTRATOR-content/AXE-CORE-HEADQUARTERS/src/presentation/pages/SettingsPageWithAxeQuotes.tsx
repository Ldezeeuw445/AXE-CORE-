import SettingsPage from '@/presentation/pages/SettingsPage';

/** Quotes live inside SettingsPage scroll (Voice → Quotes → Trust). */
export default function SettingsPageWithAxeQuotes() {
  return <SettingsPage />;
}
